 <h1>Songs</h1>

<ul>
	
  <% @songs.each do |s| %>
  	<li><%= link_to s.artist_name+" - "+s.title, song_path(s) %></li>
    <li><%= link_to s.artist_name, artist_path(s.artist_id) %></li>
    <%binding.pry%>
  <% end %>
</ul>
 