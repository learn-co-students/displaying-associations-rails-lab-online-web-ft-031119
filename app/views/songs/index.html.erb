 <h1>Songs</h1>

<ul>
	
  <% @songs.each do |s| %>
  	<li><%= link_to s.artist_name+" - "+s.title, song_path(s) %></li>
    
    <a href="<%= artist_path(s.artist) %>"> <%= s.artist_name %></a> 
    
  <% end %>
</ul>
 